include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    # La DevTools extension es un paquete Flutter aparte (con su propio
    # analysis_options); su bundle compilado no debe analizarse desde acá.
    - extension/**

